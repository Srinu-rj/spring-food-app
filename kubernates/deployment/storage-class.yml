apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: demo-storage
provisioner: k8s.io/minikube-hostpath #data will be store in minikube path
volumeBindingMode: Immediate
reclaimPolicy: Delete  # default value is Delete
---