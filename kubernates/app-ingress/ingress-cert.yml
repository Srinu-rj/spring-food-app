apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ingress-cert
  namespace: app-namespace
spec:
  secretName: ca-secret
  issuerRef:
    name: ingress-nginx-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - ingress_host_name # must match with in Ingress hostName
