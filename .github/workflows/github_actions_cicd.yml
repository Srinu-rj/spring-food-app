name: CI/CD Pipeline for Spring Boot Application

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout Code
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Set up JDK
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: 17

      # Step 3: Cache Maven Dependencies
      - name: Cache Maven Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven

      # Step 4: Build and Test Application
      - name: Build and Test
        run: mvn clean install

  docker:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: build

    steps:
      # Step 1: Checkout Code
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Log in to DockerHub
      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Step 3: Build Docker Image
      - name: Build Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/spring-image:${{ github.sha }} .

      # Step 4: Push Docker Image
      - name: Push Docker Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/spring-image:${{ github.sha }}

#      - name: Cache Qodana results
#        uses: actions/cache@v3
#        with:
#        path: ~/.cache/qodana
#        key: qodana-2024.1-${{ runner.os }}-${{ hashFiles('**/qodana.yaml') }}
#        restore-keys: |
#          qodana-2024.1-${{ runner.os }}-
#          qodana-${{ runner.os }}-
#
#      - task: Cache@2  # Not required, but Qodana will open projects with cache faster.
#        inputs:
#        key: '"$(Build.Repository.Name)" | "$(Build.SourceBranchName)" | "$(Build.SourceVersion)"'
#        path: '$(Agent.TempDirectory)/qodana/cache'
#        restoreKeys: |
#          "$(Build.Repository.Name)" | "$(Build.SourceBranchName)"
#          "$(Build.Repository.Name)"
#      - task: QodanaScan@2023
#        inputs:
#        args: -l,jetbrains/qodana-jvm-community:latest
#        env:
#         QODANA_TOKEN: $(QODANA_TOKEN)

---
name: Qodana
on:
  workflow_dispatch:
  pull_request:
  push:
    branches: # Specify your branches here
      - main # The 'main' branch
      - 'releases/*' # The release branches

jobs:
  qodana:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      checks: write
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.sha }}  # to check out the actual pull request commit, not the merge commit
          fetch-depth: 0  # a full history is required for pull request analysis
      - name: 'Qodana Scan'
        uses: JetBrains/qodana-action@v2024.2
        with:
          pr-mode: false
        env:
          QODANA_TOKEN: ${{ secrets.QODANA_TOKEN }}
          QODANA_ENDPOINT: 'https://qodana.cloud'
---

  deploy:
    name: Deploy to Kubernetes
    runs-on: ubuntu-latest
    needs: docker

#    steps:
#      # Step 1: Checkout Code
#      - name: Checkout Code
#        uses: actions/checkout@v3

#      # Step 2: Set up kubectl
#      - name: Set up kubectl
#        uses: azure/setup-kubectl@v3
#        with:
#          version: 'v1.28.0'
#
#      # Step 3: Configure Kubeconfig
#      - name: Set up Kubeconfig
#        run: |
#          echo "${{ secrets.KUBECONFIG_CONTENT }}" > kubeconfig
#          export KUBECONFIG=kubeconfig
#
#      # Step 4: Deploy to Kubernetes
#      - name: Deploy to Kubernetes
#        run: |
#          kubectl apply -f k8s-manifests/
#          kubectl set image deployment/spring-deployment spring-container=${{ secrets.DOCKER_USERNAME }}/spring-image:${{ github.sha }}
